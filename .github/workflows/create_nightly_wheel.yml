name: Build and publish stormpy nightly wheels

on:
  schedule:
    - cron: "0 2 * * *"   # nightly at 02:00 UTC
  workflow_dispatch:

permissions:
  contents: write  # required to push gh-pages
  pages: write
  id-token: write

env:
  RETENTION_DAYS: 5

jobs:
  compute-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout stormpy
        uses: actions/checkout@v6
        with:
          repository: stormchecker/stormpy
          ref: master
          path: stormpy-version
          fetch-depth: 1
          sparse-checkout: |
              lib/stormpy/_version.py

      - name: Extract base version
        id: base
        run: |
          BASE_VERSION=$(python - << 'EOF'
          from pathlib import Path
          import re

          content = Path("stormpy-version/lib/stormpy/_version.py").read_text()
          print(re.search(r'__version__\s*=\s*"([^"]+)"', content).group(1))
          EOF
          )
          echo "Base version: $BASE_VERSION"
          echo "base_version=$BASE_VERSION" >> "$GITHUB_OUTPUT"

      - name: Compute nightly version
        id: version
        run: |
          DATE=$(date -u +%Y%m%d)
          echo "Stormpy version: ${{ steps.base.outputs.base_version }}.dev${DATE}"
          echo "version=${{ steps.base.outputs.base_version }}.dev${DATE}" >> "$GITHUB_OUTPUT"

  build-wheels:
    needs: compute-version
    strategy:
      matrix:
        config:
          - build_type: Release
            version_suffix: ""
          - build_type: Debug
            version_suffix: "+debug"
    uses: stormchecker/stormpy/.github/workflows/create_wheel.yml@master
    with:
      storm_version: master
      version: ${{ needs.compute-version.outputs.version }}${{ matrix.config.version_suffix }}
      full_build: false
      build_type: ${{ matrix.config.build_type }}
      repo: stormchecker/stormpy
      ref: master

  publish:
    name: Publish nightly wheels
    needs: build-wheels
    runs-on: ubuntu-latest

    steps:
      - name: Checkout gh-pages
        uses: actions/checkout@v6
        with:
          ref: gh-pages
          path: gh-pages
      - name: Download built wheels
        uses: actions/download-artifact@v8
        with:
          path: wheelhouse
      - name: Add new nightly wheels
        run: |
          mkdir -p gh-pages/simple/stormpy
          find wheelhouse -name "*.whl" -exec cp {} gh-pages/simple/stormpy/ \;
      - name: Prune old nightly wheels (keep last 14 days)
        run: |
          python - <<'EOF'
          from pathlib import Path
          from datetime import date, timedelta
          import os
          import re
          
          # Read retention window from GitHub environment variable
          retention_days = int(os.environ.get("RETENTION_DAYS", "14"))
          cutoff = date.today() - timedelta(days=retention_days)
          # Pattern matches .devYYYYMMDD
          pattern = re.compile(r"\.dev(\d{8})")
          
          for p in Path("gh-pages/simple/stormpy").glob("*.whl"):
              m = pattern.search(p.name)
              if not m:
                  continue
              y, mth, d = int(m.group(1)[:4]), int(m.group(1)[4:6]), int(m.group(1)[6:])
              if date(y, mth, d) < cutoff:
                  p.unlink()
          EOF
      - name: Regenerate index
        run: |
          python - <<'EOF'
          from pathlib import Path
          
          pkgdir = Path("gh-pages/simple/stormpy")
          wheels = sorted(p.name for p in pkgdir.glob("*.whl"))
          with (pkgdir / "index.html").open("w") as f:
              f.write("<!DOCTYPE html>\n<html><body>\n")
              for w in wheels:
                  f.write(f'<a href="{w}">{w}</a><br/>\n')
              f.write("</body></html>\n")
          EOF
      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: gh-pages
          force_orphan: true
